<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huawei.hms.audioeditor.demo">
    <!-- 允许程序振动 UI操作的震动反馈需要 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 允许程序录音 音频录音功能需要 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- 允许程序写入外部存储, 素材存储，录音文件等本地存储需要 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VideoEditDemo">
        <activity android:name=".FileApiActivity"/>
        <activity android:name=".StreamApiActivity" />
        <activity android:name=".AudioFilePickerActivity">
            <intent-filter>
                <action android:name="com.huawei.hms.audioeditor.chooseaudio" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".SettingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AudioFormatActivity" />
        <activity android:name=".AiDubbingAudioActivity" />
        <activity android:name=".MaterialsActivity" />
        <activity android:name=".SpaceRenderActivity" />
    </application>

</manifest>